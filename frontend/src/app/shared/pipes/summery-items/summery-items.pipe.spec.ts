import { SummeryItemsPipe } from './summery-items.pipe';

describe('SummeryItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SummeryItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
